# NarrationFlow 1.8.1-rc.1

GitHub canonical sync release:
- moves preferred cross-thread recovery to the dedicated `lishehao/NarrationFlow` repository;
- adds fixed `BOOTSTRAP.md` and `latest.json` entry points;
- preserves the old `lishehao/lishehao/tools/anything-to-explainer/` mirror as compatibility fallback;
- does not assume automatic sharing of conversation-local files.
